using System;
using TypesLib;
using WatchLib;
using ArraysLib;

namespace App
{
    class Program
    {
        static void Main(string[] args)
        {
            // Test.Print();
            // Watch.Timer(System.DateTime.Now.AddSeconds(10));
            Arrays.Run();
            Arrays.RunObj();
        }
    }
}